# paramset-validator v2.3.1

Professional-grade, zero-dependency validation library for configuration objects and runtime parameter sets.

- Nested schemas
- Fluent builder API
- Schema caching & normalization
- Detailed error reporting
- Full TypeScript support
- No runtime dependencies

```bash
npm install paramset-validator
